import { ScrollLeftDirective } from './scroll-left.directive';

describe('ScrollLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollLeftDirective();
    expect(directive).toBeTruthy();
  });
});
