import { UpdateYearDirective } from './update-year.directive';

describe('UpdateYearDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateYearDirective();
    expect(directive).toBeTruthy();
  });
});
