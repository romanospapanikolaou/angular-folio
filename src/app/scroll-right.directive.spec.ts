import { ScrollRightDirective } from './scroll-right.directive';

describe('ScrollRightDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollRightDirective();
    expect(directive).toBeTruthy();
  });
});
