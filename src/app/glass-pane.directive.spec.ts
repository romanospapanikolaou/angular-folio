import { GlassPaneDirective } from './glass-pane.directive';

describe('GlassPaneDirective', () => {
  it('should create an instance', () => {
    const directive = new GlassPaneDirective();
    expect(directive).toBeTruthy();
  });
});
